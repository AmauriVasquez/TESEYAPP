# Ingresos V2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tabla de Ingresos enriquecida (orden + desglose + % por proyecto + links), un modal de pago unificado sin cascada (con progreso, anticipo/liquidación, "ya facturado"), y pago multi-proyecto con entidad depósito y factura compartida de una sola entidad.

**Architecture:** F1 solo presentación (una vista SQL + helpers + tabla, cero cambios de comportamiento). F2 refactoriza el diálogo de pago a un componente único reutilizable que solo registra dinero (se elimina la mutación de cotización/cliente). F3 agrega la entidad depósito `grupos_pago` y el modo multi-proyecto con bloqueo de factura inter-entidad.

**Tech Stack:** Vite + React 19, Supabase/Postgres, Tailwind, shadcn/radix. **Sin framework de pruebas** → verificación por tarea: `npx eslint <archivos>` (0 errores nuevos) + `npm run build` (éxito) + checks manuales + queries SQL.

**Convenciones:** rama `feat/ingresos-v2`. Migraciones/vistas aditivas aplicadas vía MCP (o pegadas por el usuario) y registradas en `supabase/migrations/`. Despliegue manual al final (build → commit dist → push → Hostinger). Móvil = tarjetas (`sm:hidden`) + tabla (`hidden sm:block`). Spec: `docs/superpowers/specs/2026-06-23-control-fiscal-facturacion-ingresos-v2-design.md`.

**Valores de referencia:** `cotizaciones.aplica_iva` boolean; `cotizaciones.branding` ∈ {tesey, iihemsa_peninsular, iihemsa}; `proyectos.costo_total` incluye IVA cuando aplica; cuentas en `src/config/cuentasPago.js` (`getCuentaLabel`, `brandingToEntidad`, `validarCobro`); `src/lib/facturacionDisplay.js` ya tiene `empresaLabel`, `marcaLabel`, `estatusFactura`.

---

## FASE 1 — Tabla enriquecida (sin cambio de comportamiento)

### Task 1: Vista SQL de progreso de pago

**Files:**
- Create: `supabase/migrations/2026-06-23_ingresos_v2_f1_view.sql`

- [ ] **Step 1: Escribir la vista**

```sql
-- 2026-06-23_ingresos_v2_f1_view.sql
CREATE OR REPLACE VIEW v_proyecto_pago_progreso AS
SELECT p.id AS proyecto_id,
       p.costo_total,
       COALESCE(SUM(pp.monto), 0) AS total_pagado,
       CASE WHEN COALESCE(p.costo_total, 0) > 0
            THEN LEAST(100, ROUND(COALESCE(SUM(pp.monto), 0) / p.costo_total * 100, 1))
            ELSE 0 END AS pct_pagado
FROM proyectos p
LEFT JOIN proyecto_pagos pp ON pp.proyecto_id = p.id
GROUP BY p.id, p.costo_total;
```

- [ ] **Step 2: Aplicar (MCP execute_sql o SQL Editor) y verificar**

```sql
SELECT * FROM v_proyecto_pago_progreso WHERE pct_pagado BETWEEN 1 AND 99 LIMIT 5;
```
Expected: filas con `pct_pagado` coherente (total_pagado/costo_total*100).

- [ ] **Step 3: Commit del archivo**

```bash
git add supabase/migrations/2026-06-23_ingresos_v2_f1_view.sql
git commit -m "feat(db): vista v_proyecto_pago_progreso (F1)"
```

---

### Task 2: Helpers de presentación (empresa completa + desglose del pago)

**Files:**
- Modify: `src/lib/facturacionDisplay.js`

- [ ] **Step 1: Empresa con nombre completo**

Reemplazar el objeto `EMPRESA_LABEL` por:
```js
const EMPRESA_LABEL = { tesey: 'Tesey', iihemsa_peninsular: 'IIHEMSA Peninsular', iihemsa: 'IIHEMSA Peninsular' };
```

- [ ] **Step 2: Helper `desglosePago`**

Agregar al final del archivo:
```js
const round2 = (n) => Math.round(Number(n) * 100) / 100;

/** Desglose DERIVADO del pago (no se almacena). Total = monto; con IVA: subtotal = total/1.16. */
export function desglosePago(monto, aplicaIva) {
  const total = round2(monto || 0);
  if (!aplicaIva) return { subtotal: total, iva: 0, total };
  const subtotal = round2(total / 1.16);
  return { subtotal, iva: round2(total - subtotal), total };
}
```

- [ ] **Step 3: Lint + commit**

```bash
npx eslint src/lib/facturacionDisplay.js
git add src/lib/facturacionDisplay.js
git commit -m "feat(finanzas): empresa nombre completo + desglosePago (F1)"
```

---

### Task 3: Modal de vista previa de cotización (solo lectura)

**Files:**
- Create: `src/components/finanzas/CotizacionPreviewDialog.jsx`

- [ ] **Step 1: Crear el componente**

```jsx
// src/components/finanzas/CotizacionPreviewDialog.jsx
import React, { useEffect, useState } from 'react';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { supabase } from '@/lib/customSupabaseClient';
import { Loader2 } from 'lucide-react';
import { empresaLabel } from '@/lib/facturacionDisplay';

export default function CotizacionPreviewDialog({ cotizacionId, open, onOpenChange }) {
  const [cot, setCot] = useState(null);
  const [items, setItems] = useState([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    if (!open || !cotizacionId) return;
    let cancelled = false;
    setLoading(true);
    (async () => {
      const [{ data: c }, { data: it }] = await Promise.all([
        supabase.from('cotizaciones').select('folio, descripcion, fecha, total, aplica_iva, estatus, branding, cliente:cliente_id(nombre), cliente_nombre_externo').eq('id', cotizacionId).single(),
        supabase.from('cotizaciones_items').select('descripcion, cantidad, unidad, precio_unitario').eq('cotizacion_id', cotizacionId).order('id'),
      ]);
      if (cancelled) return;
      setCot(c || null); setItems(it || []); setLoading(false);
    })();
    return () => { cancelled = true; };
  }, [open, cotizacionId]);

  const money = (n) => `$${Number(n || 0).toLocaleString('es-MX', { minimumFractionDigits: 2 })}`;

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="w-[95vw] max-w-2xl max-h-[90vh] overflow-y-auto sm:w-full">
        <DialogHeader><DialogTitle>Cotización {cot?.folio ?? ''}</DialogTitle></DialogHeader>
        {loading ? (
          <div className="flex justify-center py-10"><Loader2 className="w-6 h-6 animate-spin text-blue-600" /></div>
        ) : !cot ? (
          <p className="text-center py-6 text-gray-500">No se pudo cargar la cotización.</p>
        ) : (
          <div className="space-y-3 text-sm">
            <div className="grid grid-cols-2 gap-2">
              <div><span className="text-gray-500">Cliente:</span> {cot.cliente?.nombre ?? cot.cliente_nombre_externo ?? '—'}</div>
              <div><span className="text-gray-500">Empresa:</span> {empresaLabel(cot.branding)}</div>
              <div><span className="text-gray-500">Fecha:</span> {cot.fecha ?? '—'}</div>
              <div><span className="text-gray-500">Estatus:</span> {cot.estatus ?? '—'}</div>
            </div>
            <p className="text-gray-700">{cot.descripcion || ''}</p>
            <div className="border rounded overflow-x-auto">
              <table className="w-full text-xs">
                <thead className="bg-gray-50 text-gray-600"><tr>
                  <th className="text-left p-2">Descripción</th><th className="p-2">Cant.</th><th className="p-2">Unidad</th><th className="text-right p-2">P. unitario</th>
                </tr></thead>
                <tbody>
                  {items.map((it, i) => (
                    <tr key={i} className="border-t">
                      <td className="p-2">{it.descripcion}</td>
                      <td className="p-2 text-center">{it.cantidad}</td>
                      <td className="p-2 text-center">{it.unidad}</td>
                      <td className="p-2 text-right">{money(it.precio_unitario)}</td>
                    </tr>
                  ))}
                  {items.length === 0 && <tr><td colSpan={4} className="p-3 text-center text-gray-400">Sin partidas.</td></tr>}
                </tbody>
              </table>
            </div>
            <div className="flex justify-end font-semibold">Total: {money(cot.total)} {cot.aplica_iva === false ? '(sin IVA)' : '(IVA incluido)'}</div>
          </div>
        )}
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 2: Lint + build + commit**

```bash
npx eslint src/components/finanzas/CotizacionPreviewDialog.jsx && npm run build
git add src/components/finanzas/CotizacionPreviewDialog.jsx
git commit -m "feat(finanzas): CotizacionPreviewDialog solo lectura (F1)"
```

---

### Task 4: Reordenar y enriquecer la tabla de Ingresos

**Files:**
- Modify: `src/pages/Finanzas.jsx`

- [ ] **Step 1: Imports**

Asegurar imports (consolidar con los existentes de `facturacionDisplay`/`cuentasPago`):
```jsx
import { empresaLabel, marcaLabel, estatusFactura, desglosePago } from '@/lib/facturacionDisplay';
import { getCuentaLabel } from '@/config/cuentasPago';
import CotizacionPreviewDialog from '@/components/finanzas/CotizacionPreviewDialog';
import { useProyectosPathPrefix } from '@/hooks/useProyectosPathPrefix';
import { useNavigate } from 'react-router-dom';
```

- [ ] **Step 2: Enriquecer la lectura**

En el efecto `ingresosConProyecto` ampliar el `.select` de proyectos a incluir `cotizacion_id` y `cotizacion:cotizacion_id(branding, marca_comercial, aplica_iva)`. Tras obtener `proyData`, consultar la vista de progreso:
```jsx
const { data: progreso } = await supabase
  .from('v_proyecto_pago_progreso')
  .select('proyecto_id, costo_total, pct_pagado')
  .in('proyecto_id', proyectoIds);
const progPorProy = (progreso || []).reduce((a, r) => { a[r.proyecto_id] = r; return a; }, {});
```
En el `map(list...)` agregar a cada ingreso: `aplica_iva` (de `mapProy[i.proyecto_id].aplica_iva`), `cotizacion_id`, `pct_pagado` y `costo_total_proyecto` (de `progPorProy`). (Convertir el `.then` en `async` para usar `await`.)
En `mapProy` agregar a cada entrada: `aplica_iva: p?.cotizacion?.aplica_iva !== false`, `cotizacion_id: p?.cotizacion_id ?? null`.

- [ ] **Step 3: Estado para preview + navegación**

```jsx
const navigate = useNavigate();
const proyectosBase = useProyectosPathPrefix();
const [previewCotId, setPreviewCotId] = useState(null);
```

- [ ] **Step 4: Reescribir encabezados de la tabla (orden nuevo)**

Reemplazar los `<TableHead>` de la tabla de ingresos por este orden (Fecha/Cliente ordenables como hoy; los demás simples):
`Fecha · Empresa · Marca · Cliente · Cotización · Proyecto · Método · Subtotal · IVA · Total · Factura · % pago`. Mantener el sort en Fecha, Cliente, y (sobre Total) Monto.

- [ ] **Step 5: Reescribir las celdas de cada fila**

```jsx
const dg = desglosePago(i.monto, i.aplica_iva);
// celdas, en orden:
<TableCell className="whitespace-nowrap">{formatDateTable(i.fecha || i.fecha_pago)}</TableCell>
<TableCell>{empresaLabel(i.empresa)}</TableCell>
<TableCell>{marcaLabel(i.marca)}</TableCell>
<TableCell>{i.cliente}</TableCell>
<TableCell>{i.cotizacion_id ? <button className="text-blue-600 hover:underline" onClick={() => setPreviewCotId(i.cotizacion_id)}>Ver</button> : '—'}</TableCell>
<TableCell>{i.proyecto_folio ? <button className="text-blue-600 hover:underline" onClick={() => navigate(`${proyectosBase}/${i.proyecto_id}`)}>{i.proyecto_folio}</button> : '—'}</TableCell>
<TableCell className="whitespace-nowrap">{getCuentaLabel(i.cuenta_value || i.metodo_pago)}</TableCell>
<TableCell className="text-right">${dg.subtotal.toLocaleString('es-MX', { minimumFractionDigits: 2 })}</TableCell>
<TableCell className="text-right">${dg.iva.toLocaleString('es-MX', { minimumFractionDigits: 2 })}</TableCell>
<TableCell className="text-right font-medium text-green-700">${dg.total.toLocaleString('es-MX', { minimumFractionDigits: 2 })}</TableCell>
<TableCell className="text-center">{/* bandera estatusFactura(i) como en V1 */}</TableCell>
<TableCell className="text-right">
  <span className="font-medium">{Math.round(i.pct_pagado ?? 0)}%</span>
  <span className="block text-[10px] text-gray-400">de ${Number(i.costo_total_proyecto || 0).toLocaleString('es-MX')}</span>
</TableCell>
```
(`i.proyecto_folio` debe poblarse en el map: separa `folio` de la descripción que hoy se concatena en `i.proyecto`.) Actualizar `colSpan` del estado vacío a 12.

- [ ] **Step 6: Tarjetas móvil con los campos nuevos** (actualizar el bloque `sm:hidden` existente para incluir empresa completa, método, total, % y links).

- [ ] **Step 7: Render del preview**

```jsx
<CotizacionPreviewDialog cotizacionId={previewCotId} open={!!previewCotId} onOpenChange={(o) => { if (!o) setPreviewCotId(null); }} />
```

- [ ] **Step 8: Lint + build + verificación manual**

`npx eslint src/pages/Finanzas.jsx && npm run build`. En la app: la tabla muestra el orden nuevo; "Ver" abre la cotización; el folio navega al proyecto; Subtotal/IVA/Total cuadran (IVA=0 sin IVA); % por proyecto con ancla.

- [ ] **Step 9: Commit (cierre F1)**

```bash
git add src/pages/Finanzas.jsx
git commit -m "feat(finanzas): tabla de Ingresos V2 — orden, desglose, links, % por proyecto (F1)"
```

---

## FASE 2 — Modal de pago unificado, single-proyecto, sin cascada

> Refactor de `src/components/proyectos/RegistrarPagoDialog.jsx` hacia un componente reutilizable. Leer el archivo actual antes de cada tarea. Mantener funcionando `ProyectoDetalle` en cada paso.

### Task 5: Eliminar la cascada (cliente/IVA) del diálogo de pago

**Files:**
- Modify: `src/components/proyectos/RegistrarPagoDialog.jsx`

- [ ] **Step 1:** Quitar del JSX el Combobox de **Cliente** y el Switch **"Aplicar IVA"** mutadores (columna "Configuración del Proyecto"). El IVA se muestra **read-only** desde la cotización (texto: "IVA: aplica / no aplica").
- [ ] **Step 2:** Eliminar de `doSave()` el bloque `if (isDirty …)` que hace `update` a `proyectos` y `cotizaciones`, y el `AlertDialog` de confirmación de cambios. Eliminar estados `clienteId`, `aplicaIva` (mutador), `isDirty`, `confirmDialogOpen` y sus efectos.
- [ ] **Step 3:** Conservar el resumen financiero (Subtotal/IVA/Total) como **lectura** desde la cotización (sin recalcular ni guardar).
- [ ] **Step 4:** `npx eslint` + `npm run build`; verificar en ProyectoDetalle que registrar/editar pago ya **no** ofrece cambiar cliente ni IVA y guarda bien.
- [ ] **Step 5:** Commit: `feat(finanzas): el diálogo de pago ya no muta cotización/cliente (F2)`.

### Task 6: Receptora visible + barra de progreso en vivo

**Files:**
- Modify: `src/components/proyectos/RegistrarPagoDialog.jsx`

- [ ] **Step 1:** Cargar en el efecto de cotización también `branding`; calcular `pagadoActual` (suma de pagos del proyecto, excluyendo el pago en edición) — pasar `pagos`/`totalPagado` como prop desde `ProyectoDetalle`, o consultar `proyecto_pagos` por `proyecto_id`.
- [ ] **Step 2:** Bloque **Receptora** (read-only): `empresaLabel(branding)` (empresa del proyecto) + entidad de la cuenta elegida (`getCuenta(metodoPago)?.entidad`).
- [ ] **Step 3:** **Barra de progreso**: `pagadoProyectado = pagadoActual + Number(monto||0)`; `pct = clamp(pagadoProyectado / costo_total * 100)`; mostrar `% · Pagado $… · Saldo $…`, en vivo con el monto.
- [ ] **Step 4:** Lint + build + manual; Commit: `feat(finanzas): receptora + barra de progreso en el modal de pago (F2)`.

### Task 7: Anticipo % y Liquidación inteligentes

**Files:**
- Modify: `src/components/proyectos/RegistrarPagoDialog.jsx`

- [ ] **Step 1:** Si `tipoIngreso === 'Anticipo'`: mostrar campo `%`; al cambiarlo, `setMonto(round2(pct/100 * costo_total))`. Si el usuario edita el monto, recalcular el `%` mostrado desde el monto (`monto/costo_total*100`). (Chips opcionales 25/50/100.)
- [ ] **Step 2:** Si `tipoIngreso === 'Liquidación'`: al elegirlo, `setMonto(saldoPendiente)`; editable. Si al guardar `monto !== saldoPendiente` → toast de aviso "La liquidación no cierra el saldo" (no bloquea).
- [ ] **Step 3:** Solo al **crear** (no en edición de un pago existente).
- [ ] **Step 4:** Lint + build + manual; Commit: `feat(finanzas): anticipo % y liquidación sugeridos editables (F2)`.

### Task 8: Sección "Ya facturado"

**Files:**
- Modify: `src/components/proyectos/RegistrarPagoDialog.jsx`

- [ ] **Step 1:** Switch "Este pago ya está facturado" → revela `Folio *`, `Fecha *`, `Emisora` (default `brandingToEntidad(branding)`, editable con `ENTIDADES`).
- [ ] **Step 2:** En `doSave()`, tras insertar el cobro y obtener su `id`, si el switch está activo llamar `registrarFactura({ proyectoId, empresaEmisora, numero, fechaEmision, cobroIds: [nuevoCobroId] })` (reusa `facturasService`). Manejar error de folio duplicado (mensaje claro).
- [ ] **Step 3:** Lint + build + manual (registrar pago con factura → la bandera del ingreso queda "Facturado"); Commit: `feat(finanzas): registrar factura desde el diálogo de pago (F2)`.

### Task 9: Unificar — Finanzas usa el modal rico; retirar el simple

**Files:**
- Modify: `src/pages/Finanzas.jsx`

- [ ] **Step 1:** Importar `RegistrarPagoDialog`. El botón "Registrar Movimiento" abre un selector de proyecto (Combobox existente con `proyectosOptions`); al elegir, abrir `RegistrarPagoDialog` con ese `proyectoId`/`proyecto` (cargar el proyecto con `cotizacion:cotizacion_id(branding, aplica_iva, total)`).
- [ ] **Step 2:** Eliminar el `<Dialog>` "Registrar Movimiento (Ingreso)" simple y sus estados (`movimientoDialogOpen`, `ingresoForm`, `handleRegistrarIngreso`) ya cubiertos por el modal rico.
- [ ] **Step 3:** Tras guardar, refrescar (`fetchDatos`, `fetchChartData`, `fetchDatosCuentasPorCobrar`).
- [ ] **Step 4:** Lint + build + manual (registrar ingreso desde Finanzas usa el mismo modal que el proyecto); Commit (cierre F2): `feat(finanzas): Finanzas reutiliza el modal de pago unificado (F2)`.

---

## FASE 3 — Pago multi-proyecto (entidad depósito + factura compartida)

### Task 10: Migración — grupos_pago + grupo_pago_id + facturas.proyecto_id nullable

**Files:**
- Create: `supabase/migrations/2026-06-23_ingresos_v2_f3.sql`

- [ ] **Step 1:** Escribir el SQL (sección 4.1 del spec: `grupos_pago`, `proyecto_pagos.grupo_pago_id`, `ALTER facturas ALTER COLUMN proyecto_id DROP NOT NULL`, RLS de `grupos_pago`).
- [ ] **Step 2:** Aplicar (MCP/SQL Editor) y verificar: `SELECT to_regclass('public.grupos_pago');` no nulo; `proyecto_pagos.grupo_pago_id` existe; `facturas.proyecto_id` ahora `is_nullable=YES`.
- [ ] **Step 3:** Commit del archivo.

### Task 11: facturasService — multi-cobro con bloqueo de misma entidad

**Files:**
- Modify: `src/services/facturasService.js`

- [ ] **Step 1:** Nueva función `registrarFacturaMulti({ empresaEmisora, numero, fechaEmision, monto, cobroIds })`: antes de insertar, consultar los proyectos de esos `cobroIds` y su `branding`; si `brandingToEntidad` no es homogéneo → devolver `{ error: { message: 'Una factura no puede abarcar proyectos de distinta entidad.' } }`. Si ok, insertar `facturas` (proyecto_id NULL) y `update proyecto_pagos.factura_id` para todos.
- [ ] **Step 2:** Lint + commit: `feat(finanzas): registrarFacturaMulti con bloqueo de entidad (F3)`.

### Task 12: Modo "Varios proyectos" en el modal

**Files:**
- Modify: `src/components/proyectos/RegistrarPagoDialog.jsx` (o un subcomponente `PagoMultiProyecto.jsx`)

- [ ] **Step 1:** En el modal abierto desde Finanzas, un botón/modo "Varios proyectos" que cambia a una UI de asignación: agregar proyectos (Combobox) con un input de monto cada uno; mostrar `total = suma`; aviso si el monto de un proyecto excede su saldo (consultar `v_proyecto_pago_progreso`/saldo).
- [ ] **Step 2:** Sección compartida: fecha, cuenta (método), y opcional "Ya facturado" (folio/fecha/emisora) para todo el grupo.
- [ ] **Step 3:** Lint + build; Commit: `feat(finanzas): UI de pago multi-proyecto (F3)`.

### Task 13: Guardar multi-proyecto (grupo + N cobros + factura compartida)

**Files:**
- Modify: `src/services/facturasService.js` (o nuevo `src/services/pagosService.js`)
- Modify: `src/components/proyectos/RegistrarPagoDialog.jsx`

- [ ] **Step 1:** Función `registrarPagoMultiProyecto({ fecha, cuentaValue, asignaciones: [{proyectoId, monto}], factura? })`: inserta `grupos_pago` (monto_total = suma), luego N `proyecto_pagos` con ese `grupo_pago_id`/`cuenta_value`; recupera los `id` de cobros; si `factura` → `registrarFacturaMulti(... cobroIds)`.
- [ ] **Step 2:** Conectar el botón Guardar del modo multi a esta función; refrescar al terminar.
- [ ] **Step 3:** Lint + build + manual: registrar un pago a 2 proyectos de la **misma** entidad con factura compartida → ambos cobros quedan "Facturado · folio"; intentar 2 entidades distintas con factura → error claro.
- [ ] **Step 4:** Commit (cierre F3): `feat(finanzas): pago multi-proyecto con grupo y factura compartida (F3)`.

---

## Verificación final
- [ ] F1: tabla con orden/columnas nuevas, links funcionan, desglose e % correctos; móvil en tarjetas.
- [ ] F2: registrar pago NO cambia cliente/IVA; receptora visible; barra de progreso; anticipo %/liquidación; "ya facturado"; Finanzas usa el mismo modal.
- [ ] F3: multi-proyecto reparte y crea grupo + N cobros; factura compartida solo misma entidad (bloqueo probado).
- [ ] `npm run build` limpio; sin regresiones en ProyectoDetalle.

## Riesgos (del pressure-test)
1. F2 toca un componente grande y en uso (ProyectoDetalle): verificar en cada tarea que sigue guardando pagos.
2. `facturas.proyecto_id` nullable: revisar `RegistrarFacturaDialog`/`facturasService` de V1 (siguen sirviendo el caso single).
3. Multi-proyecto es el modo más complejo: mantenerlo separado del flujo single.
4. Performance del % vía vista: una consulta por los proyectos visibles, no por fila.
